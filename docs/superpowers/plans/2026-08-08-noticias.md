# Seção de Notícias (Tech/IA) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Criar `/noticias`, uma página que agrega manchetes de tecnologia/IA do GNews e WorldNewsAPI, filtra notícias de viés negativo, e é atualizada 1x/dia via cron na VPS.

**Architecture:** Script standalone (`scripts/ingest-news.mjs`) roda via crontab na VPS 1x/dia, busca as duas APIs, filtra e grava num SQLite local (`node:sqlite`, nativo do Node, sem dependência externa). A página `/noticias` (Server Component) lê esse SQLite a cada request. Sem Supabase, sem endpoint HTTP de ingestão exposto.

**Tech Stack:** Next.js App Router (Server Components), `node:sqlite` (Node 24+, nativo), Node `fetch` nativo, TypeScript + JS puro (script `.mjs`).

## Global Constraints

- Node 24+ obrigatório (confirmado local e VPS) — `node:sqlite` e import direto de `.ts` a partir de `.mjs` dependem disso.
- Nunca armazenar ou exibir o texto completo de um artigo — só `title` + snippet curto (~200 caracteres) + link externo.
- `GNEWS_API_KEY` e `WORLDNEWS_API_KEY` são secrets server-only — **sem** prefixo `NEXT_PUBLIC_`, nunca no cliente.
- Banco em `data/news.db` (raiz do projeto) — não versionado, entra no `.gitignore`.
- Janela de retenção: 15 dias (`published_at`).
- Termos de busca fixos: `['inteligência artificial', 'tecnologia']`.
- Sem framework de testes no projeto — verificação é manual (scripts `node -e`, `tsc --noEmit`, `npm run build`, checagem em navegador), seguindo o padrão já usado no resto do repositório.

---

### Task 1: Filtro de palavras negativas

**Files:**
- Create: `lib/news-filters.ts`

**Interfaces:**
- Produces: `isNegativeNews(title: string, summary: string): boolean` — usado pelo script de ingestão (Task 3).

- [ ] **Step 1: Criar o módulo de filtro**

```typescript
// lib/news-filters.ts
const NEGATIVE_KEYWORDS = [
  'demiss', 'demite', 'demitiu', 'corte de vaga', 'corte de emprego', 'corte de custo',
  'desliga funcion', 'reducao de pessoal', 'layoff', 'vazamento de dados',
  'ataque hacker', 'ciberataque', 'invasao', 'processo judicial', 'acao judicial',
  'multa', 'multad', 'prejuizo', 'preju', 'crise', 'fraude', 'falencia', 'recall',
  'escandal', 'polemic', 'acusad', 'banid', 'proibid', 'colapso', 'queda livre',
  'hackead', 'golpe', 'phishing', 'malware', 'ransomware', 'censura', 'boicote',
  'greve', 'protesto', 'denuncia', 'investigacao', 'apagao', 'falha grave',
  'bug critico', 'inseguranca', 'plagio', 'discrimina', 'desemprego',
];

function normalize(text: string): string {
  return text
    .normalize('NFD')
    .replace(/[̀-ͯ]/g, '')
    .toLowerCase();
}

export function isNegativeNews(title: string, summary: string): boolean {
  const haystack = normalize(`${title} ${summary}`);
  return NEGATIVE_KEYWORDS.some((keyword) => haystack.includes(keyword));
}
```

- [ ] **Step 2: Verificar manualmente (sem framework de testes no projeto)**

Run:
```bash
node -e "
import('./lib/news-filters.ts').then(({ isNegativeNews }) => {
  console.log('deve ser true:', isNegativeNews('Microsoft demite 10 mil funcionários após adoção de IA', ''));
  console.log('deve ser false:', isNegativeNews('Nova IA generativa acelera diagnósticos médicos', 'Hospitais adotam tecnologia para agilizar exames.'));
  console.log('deve ser true (acento/maiúscula):', isNegativeNews('EMPRESA ANUNCIA DEMISSÃO em massa', ''));
});
"
```
Expected: as três linhas impressas como `true`, `false`, `true`.

- [ ] **Step 3: Commit**

```bash
git add lib/news-filters.ts
git commit -m "feat(noticias): filtro de palavras-chave negativas"
```

---

### Task 2: Módulo de banco (SQLite)

**Files:**
- Create: `lib/news-db.ts`
- Modify: `.gitignore` (adicionar `/data`)

**Interfaces:**
- Produces:
  - `interface NewsItem { id: string; source: string; sourceName: string; title: string; summary: string; url: string; imageUrl: string | null; publishedAt: string; }`
  - `getRecentNews(): NewsItem[]` — usado pela página (Task 4).
  - `upsertNews(items: NewsItem[], fetchedAt: string): number` — usado pelo script de ingestão (Task 3). Retorna quantos itens novos foram inseridos.
  - `pruneOldNews(maxAgeDays: number): number` — usado pelo script de ingestão (Task 3). Retorna quantos itens foram removidos.

- [ ] **Step 1: Criar o módulo**

```typescript
// lib/news-db.ts
import { DatabaseSync } from 'node:sqlite';
import { mkdirSync } from 'node:fs';
import { dirname, join } from 'node:path';

export interface NewsItem {
  id: string;
  source: string;
  sourceName: string;
  title: string;
  summary: string;
  url: string;
  imageUrl: string | null;
  publishedAt: string;
}

const DB_PATH = join(process.cwd(), 'data', 'news.db');

function getDb(): DatabaseSync {
  mkdirSync(dirname(DB_PATH), { recursive: true });
  const db = new DatabaseSync(DB_PATH);
  db.exec(`
    CREATE TABLE IF NOT EXISTS news (
      id TEXT PRIMARY KEY,
      source TEXT NOT NULL,
      source_name TEXT,
      title TEXT NOT NULL,
      summary TEXT,
      url TEXT NOT NULL UNIQUE,
      image_url TEXT,
      published_at TEXT NOT NULL,
      fetched_at TEXT NOT NULL
    );
    CREATE INDEX IF NOT EXISTS idx_news_published_at ON news(published_at DESC);
  `);
  return db;
}

interface NewsRow {
  id: string;
  source: string;
  source_name: string;
  title: string;
  summary: string;
  url: string;
  image_url: string | null;
  published_at: string;
}

export function getRecentNews(): NewsItem[] {
  const db = getDb();
  try {
    const rows = db.prepare('SELECT * FROM news ORDER BY published_at DESC').all() as unknown as NewsRow[];
    return rows.map((row) => ({
      id: row.id,
      source: row.source,
      sourceName: row.source_name,
      title: row.title,
      summary: row.summary,
      url: row.url,
      imageUrl: row.image_url,
      publishedAt: row.published_at,
    }));
  } finally {
    db.close();
  }
}

export function upsertNews(items: NewsItem[], fetchedAt: string): number {
  const db = getDb();
  try {
    const insert = db.prepare(`
      INSERT OR IGNORE INTO news (id, source, source_name, title, summary, url, image_url, published_at, fetched_at)
      VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)
    `);
    let inserted = 0;
    for (const item of items) {
      const result = insert.run(
        item.id, item.source, item.sourceName, item.title, item.summary,
        item.url, item.imageUrl, item.publishedAt, fetchedAt
      );
      inserted += Number(result.changes);
    }
    return inserted;
  } finally {
    db.close();
  }
}

export function pruneOldNews(maxAgeDays: number): number {
  const db = getDb();
  try {
    const cutoff = new Date(Date.now() - maxAgeDays * 24 * 60 * 60 * 1000).toISOString();
    const result = db.prepare('DELETE FROM news WHERE published_at < ?').run(cutoff);
    return Number(result.changes);
  } finally {
    db.close();
  }
}
```

- [ ] **Step 2: Adicionar `/data` ao `.gitignore`**

Adicionar ao final de `.gitignore`:
```
# news database (gerado pelo script de ingestão, não versionado)
/data
```

- [ ] **Step 3: Verificar manualmente**

Run:
```bash
node -e "
import('./lib/news-db.ts').then(({ upsertNews, getRecentNews, pruneOldNews }) => {
  const now = new Date().toISOString();
  const old = new Date(Date.now() - 20 * 24 * 60 * 60 * 1000).toISOString();
  const inserted = upsertNews([
    { id: 'test-1', source: 'gnews', sourceName: 'Teste', title: 'Notícia recente', summary: 'resumo', url: 'https://example.com/1', imageUrl: null, publishedAt: now },
    { id: 'test-2', source: 'gnews', sourceName: 'Teste', title: 'Notícia antiga', summary: 'resumo', url: 'https://example.com/2', imageUrl: null, publishedAt: old },
  ], now);
  console.log('inseridos (esperado 2):', inserted);
  console.log('total antes de prune (esperado >= 2):', getRecentNews().length);
  const pruned = pruneOldNews(15);
  console.log('removidos por idade (esperado >= 1):', pruned);
  console.log('total depois de prune:', getRecentNews().length);
});
"
```
Expected: `inseridos: 2`, contagens condizentes, e a notícia antiga desaparecendo após o prune.

Limpar o banco de teste antes de seguir: `rm -f data/news.db`

- [ ] **Step 4: Commit**

```bash
git add lib/news-db.ts .gitignore
git commit -m "feat(noticias): módulo de banco SQLite (leitura/escrita/expiração)"
```

---

### Task 3: Script de ingestão

**Files:**
- Create: `scripts/ingest-news.mjs`
- Modify: `.env.example` (adicionar `GNEWS_API_KEY` e `WORLDNEWS_API_KEY`)

**Interfaces:**
- Consumes: `isNegativeNews` (Task 1), `upsertNews`/`pruneOldNews` (Task 2).
- Produces: nenhuma interface nova — é o ponto de entrada do cron.

- [ ] **Step 1: Criar o script**

```javascript
#!/usr/bin/env node
/**
 * News Ingestion Script
 * Busca notícias de tech/IA no GNews e WorldNewsAPI, filtra viés negativo,
 * deduplica e grava no SQLite local.
 *
 * Usage: node --env-file=.env.local scripts/ingest-news.mjs
 */

import { createHash } from 'node:crypto';
import { isNegativeNews } from '../lib/news-filters.ts';
import { upsertNews, pruneOldNews } from '../lib/news-db.ts';

const SEARCH_TERMS = ['inteligência artificial', 'tecnologia'];
const MAX_AGE_DAYS = 15;
const GNEWS_API_KEY = process.env.GNEWS_API_KEY;
const WORLDNEWS_API_KEY = process.env.WORLDNEWS_API_KEY;

function hashUrl(url) {
  return createHash('sha256').update(url).digest('hex');
}

function normalizeTitle(title) {
  return title
    .normalize('NFD')
    .replace(/[̀-ͯ]/g, '')
    .toLowerCase()
    .replace(/[^\w\s]/g, '')
    .trim();
}

function truncate(text, maxLength) {
  if (!text) return '';
  return text.length > maxLength ? `${text.slice(0, maxLength).trim()}...` : text;
}

function extractSourceName(url) {
  try {
    return new URL(url).hostname.replace(/^www\./, '');
  } catch {
    return 'Fonte externa';
  }
}

async function fetchGNews(term) {
  const url = `https://gnews.io/api/v4/search?q=${encodeURIComponent(term)}&lang=pt&max=10&apikey=${GNEWS_API_KEY}`;
  try {
    const response = await fetch(url);
    if (!response.ok) {
      console.error(`[GNews] "${term}": HTTP ${response.status}`);
      return [];
    }
    const data = await response.json();
    return (data.articles || []).map((article) => ({
      source: 'gnews',
      sourceName: article.source?.name || 'GNews',
      title: article.title,
      summary: truncate(article.description, 200),
      url: article.url,
      imageUrl: article.image || null,
      publishedAt: article.publishedAt,
    }));
  } catch (error) {
    console.error(`[GNews] "${term}": ${error.message}`);
    return [];
  }
}

async function fetchWorldNews(term) {
  const earliestDate = new Date(Date.now() - MAX_AGE_DAYS * 24 * 60 * 60 * 1000)
    .toISOString()
    .slice(0, 10);
  const url = `https://api.worldnewsapi.com/search-news?text=${encodeURIComponent(term)}&language=pt&number=10&earliest-publish-date=${earliestDate}&api-key=${WORLDNEWS_API_KEY}`;
  try {
    const response = await fetch(url);
    if (!response.ok) {
      console.error(`[WorldNewsAPI] "${term}": HTTP ${response.status}`);
      return [];
    }
    const data = await response.json();
    return (data.news || []).map((item) => ({
      source: 'worldnews',
      sourceName: extractSourceName(item.url),
      title: item.title,
      summary: truncate(item.summary || item.text, 200),
      url: item.url,
      imageUrl: item.image || null,
      publishedAt: item.publish_date,
    }));
  } catch (error) {
    console.error(`[WorldNewsAPI] "${term}": ${error.message}`);
    return [];
  }
}

async function main() {
  if (!GNEWS_API_KEY || !WORLDNEWS_API_KEY) {
    console.error('GNEWS_API_KEY e/ou WORLDNEWS_API_KEY não configuradas. Abortando.');
    process.exit(1);
  }

  const results = [];
  for (const term of SEARCH_TERMS) {
    const [gnews, worldnews] = await Promise.all([fetchGNews(term), fetchWorldNews(term)]);
    results.push(...gnews, ...worldnews);
  }
  console.log(`Buscados: ${results.length} itens brutos`);

  const seenUrls = new Set();
  const seenTitles = new Set();
  const deduped = [];
  for (const item of results) {
    if (!item.title || !item.url || !item.publishedAt) continue;
    const titleKey = normalizeTitle(item.title);
    if (seenUrls.has(item.url) || seenTitles.has(titleKey)) continue;
    seenUrls.add(item.url);
    seenTitles.add(titleKey);
    deduped.push(item);
  }
  console.log(`Após deduplicação: ${deduped.length} itens`);

  const filtered = deduped.filter((item) => !isNegativeNews(item.title, item.summary));
  console.log(`Após filtro negativo: ${filtered.length} itens (${deduped.length - filtered.length} descartados)`);

  const cutoffMs = Date.now() - MAX_AGE_DAYS * 24 * 60 * 60 * 1000;
  const withinWindow = filtered.filter((item) => {
    const publishedMs = new Date(item.publishedAt).getTime();
    return !Number.isNaN(publishedMs) && publishedMs >= cutoffMs;
  });
  console.log(`Dentro da janela de ${MAX_AGE_DAYS} dias: ${withinWindow.length} itens`);

  const items = withinWindow.map((item) => ({ id: hashUrl(item.url), ...item }));

  const fetchedAt = new Date().toISOString();
  const inserted = upsertNews(items, fetchedAt);
  console.log(`Gravados no banco: ${inserted} novos itens`);

  const pruned = pruneOldNews(MAX_AGE_DAYS);
  console.log(`Removidos por expiração (>${MAX_AGE_DAYS} dias): ${pruned} itens`);
}

main().catch((error) => {
  console.error('Erro fatal na ingestão:', error);
  process.exit(1);
});
```

- [ ] **Step 2: Adicionar as chaves ao `.env.example`**

Adicionar:
```
# News APIs (GNews + WorldNewsAPI) — server-only, secret
GNEWS_API_KEY=your-gnews-api-key
WORLDNEWS_API_KEY=your-worldnews-api-key
```

- [ ] **Step 3: Adicionar as chaves reais ao `.env.local` (não commitado)**

As chaves reais foram fornecidas pelo usuário no chat (não reproduzidas
aqui — este arquivo é versionado no git). Adicionar ao `.env.local` (local
e, depois no deploy, na VPS), seguindo o formato de `.env.example`:
```
GNEWS_API_KEY=<chave real, ver .env.local>
WORLDNEWS_API_KEY=<chave real, ver .env.local>
```

- [ ] **Step 4: Rodar o script de verdade e conferir**

Run: `node --env-file=.env.local scripts/ingest-news.mjs`

Expected: log mostrando itens buscados > 0, deduplicados, filtrados, e "Gravados no banco: N novos itens" com N > 0 (salvo se as APIs não tiverem retornado nada tech/IA em português no momento — improvável, mas se acontecer, checar a saída de erro de cada API antes de prosseguir).

Conferir que nada de negativo escapou:
```bash
node -e "
import('./lib/news-db.ts').then(({ getRecentNews }) => {
  const news = getRecentNews();
  console.log('Total no banco:', news.length);
  news.slice(0, 5).forEach(n => console.log('-', n.title, '|', n.sourceName));
});
"
```

- [ ] **Step 5: Rodar de novo para confirmar idempotência**

Run: `node --env-file=.env.local scripts/ingest-news.mjs`

Expected: "Gravados no banco: 0 novos itens" (ou um número bem menor que a primeira rodada, já que a maioria das notícias já foi inserida — `INSERT OR IGNORE` não duplica pela `url`).

- [ ] **Step 6: Commit**

```bash
git add scripts/ingest-news.mjs .env.example
git commit -m "feat(noticias): script de ingestão (GNews + WorldNewsAPI)"
```

---

### Task 4: Página `/noticias`

**Files:**
- Create: `app/noticias/page.tsx`

**Interfaces:**
- Consumes: `getRecentNews(): NewsItem[]` (Task 2), `TrackedCTA` (`components/ui/TrackedCTA.tsx`, já existe).

- [ ] **Step 1: Criar a página**

```tsx
// app/noticias/page.tsx
import type { Metadata } from 'next';
import Image from 'next/image';
import { getRecentNews } from '@/lib/news-db';
import TrackedCTA from '@/components/ui/TrackedCTA';

export const metadata: Metadata = {
  title: 'Notícias de Tecnologia e IA | Dexi Digital',
  description:
    'Acompanhe as principais notícias de tecnologia e inteligência artificial, selecionadas diariamente pela Dexi Digital.',
};

export const dynamic = 'force-dynamic';

function formatRelativeDate(isoDate: string): string {
  const date = new Date(isoDate);
  const diffMs = Date.now() - date.getTime();
  const diffDays = Math.floor(diffMs / (1000 * 60 * 60 * 24));
  if (diffDays <= 0) return 'Hoje';
  if (diffDays === 1) return 'Há 1 dia';
  return `Há ${diffDays} dias`;
}

export default function NoticiasPage() {
  const news = getRecentNews();

  return (
    <main className="min-h-screen">
      <section className="section-hero-premium py-24 md:py-32 border-b border-[var(--border-subtle)]">
        <div className="container">
          <div className="max-w-4xl mx-auto text-center">
            <p className="text-overline mb-4">Radar Dexi</p>
            <h1 className="text-display-xl md:text-display-2xl text-[var(--text-primary)] mb-6">
              <span className="text-display-gradient">Notícias de Tecnologia e IA</span>
            </h1>
            <p className="text-lead max-w-2xl mx-auto">
              Uma curadoria diária das principais notícias sobre tecnologia e inteligência artificial, direto das fontes originais.
            </p>
          </div>
        </div>
      </section>

      <section className="section bg-[var(--bg-surface)]">
        <div className="container">
          <div className="max-w-6xl mx-auto">
            <div className="mb-10 text-center">
              <TrackedCTA type="validation" location="noticias" variant="money" text="Agendar diagnóstico gratuito" />
            </div>

            {news.length === 0 ? (
              <div className="text-center py-16">
                <p className="text-body text-[var(--text-secondary)]">
                  Estamos preparando as próximas notícias. Volte em breve.
                </p>
              </div>
            ) : (
              <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
                {news.map((item) => (
                  <article key={item.id} className="card card-interactive overflow-hidden flex flex-col">
                    <div className="relative h-44 w-full bg-[var(--bg-primary)]">
                      {item.imageUrl ? (
                        <Image
                          src={item.imageUrl}
                          alt={item.title}
                          fill
                          className="object-cover"
                          unoptimized
                        />
                      ) : (
                        <div className="section-gradient absolute inset-0 flex items-center justify-center">
                          <Image
                            src="/images/brand/Dexi White Icon Transparent bg.svg"
                            alt=""
                            width={48}
                            height={48}
                            className="opacity-80"
                          />
                        </div>
                      )}
                    </div>
                    <div className="p-6 flex flex-col flex-1">
                      <div className="flex items-center gap-2 mb-3">
                        <span className="px-3 py-1 text-xs font-semibold bg-[var(--color-primary-100)] dark:bg-[var(--color-primary-900)]/30 text-[var(--color-primary-700)] dark:text-[var(--color-primary-300)] rounded-full">
                          {item.sourceName}
                        </span>
                        <span className="text-xs text-[var(--text-muted)]">{formatRelativeDate(item.publishedAt)}</span>
                      </div>
                      <h2 className="text-h5 mb-3">{item.title}</h2>
                      {item.summary && (
                        <p className="text-body-sm text-[var(--text-secondary)] mb-4 flex-1">{item.summary}</p>
                      )}
                      <a
                        href={item.url}
                        target="_blank"
                        rel="noopener noreferrer"
                        className="text-sm font-semibold text-[var(--color-primary-600)] hover:text-[var(--color-primary-700)] transition-base inline-flex items-center gap-1 mt-auto"
                      >
                        Leia em {item.sourceName}
                        <svg className="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                          <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M17 8l4 4m0 0l-4 4m4-4H3" />
                        </svg>
                      </a>
                    </div>
                  </article>
                ))}
              </div>
            )}
          </div>
        </div>
      </section>
    </main>
  );
}
```

- [ ] **Step 2: Verificar tipos**

Run: `npx tsc --noEmit`
Expected: sem erros.

- [ ] **Step 3: Rodar localmente e conferir no navegador**

Run: `npm run dev`, abrir `http://localhost:3000/noticias` (ou 3001, conforme a porta que subir).

Expected: grid de cards com notícias reais (gravadas na Task 3), CTA "Agendar diagnóstico gratuito" visível, links "Leia em [fonte]" abrindo em nova aba.

- [ ] **Step 4: Commit**

```bash
git add app/noticias/page.tsx
git commit -m "feat(noticias): página /noticias"
```

---

### Task 5: Navegação e SEO

**Files:**
- Modify: `components/layout/Footer.tsx`
- Modify: `app/sitemap.ts`

**Interfaces:**
- Nenhuma nova — só integração.

- [ ] **Step 1: Adicionar link no footer**

Em `components/layout/Footer.tsx`, dentro de `navigation.company`, adicionar após `Blog`:

```typescript
company: [
  { name: 'Sobre', href: '/sobre' },
  { name: 'Cases', href: '/cases' },
  { name: 'Blog', href: '/blog' },
  { name: 'Notícias', href: '/noticias' },
  { name: 'Contato', href: '/contato' },
],
```

- [ ] **Step 2: Adicionar ao sitemap**

Em `app/sitemap.ts`, adicionar (perto de `blogPages`):

```typescript
  // Notícias
  const newsPages: MetadataRoute.Sitemap = [
    {
      url: `${baseUrl}/noticias`,
      lastModified: currentDate,
      changeFrequency: 'daily',
      priority: 0.6,
    },
  ];
```

E incluir `...newsPages,` no array retornado (junto de `...blogPages,`).

- [ ] **Step 3: Verificar**

Run: `npx tsc --noEmit`
Expected: sem erros.

Conferir visualmente no navegador (`npm run dev`) que "Notícias" aparece no rodapé e leva para `/noticias`.

- [ ] **Step 4: Commit**

```bash
git add components/layout/Footer.tsx app/sitemap.ts
git commit -m "feat(noticias): link no footer e entrada no sitemap"
```

---

### Task 6: Deploy e cron na VPS

**Files:** nenhum arquivo novo — só operação (push, configuração de ambiente, cron).

- [ ] **Step 1: Push para o GitHub**

```bash
git push origin master
```

- [ ] **Step 2: Configurar as chaves no `.env.local` da VPS**

Via SSH (`root@194.146.12.158`, chave `~/.ssh/dexi_vps`), adicionar ao
`.env.local` existente em `/root/dexidigital.com.br/.env.local` as mesmas
`GNEWS_API_KEY` e `WORLDNEWS_API_KEY` já configuradas no `.env.local` local
(copiar o arquivo/valores diretamente, sem digitar a chave em nenhum
comando de shell — mesmo padrão de segurança usado para as chaves do
OpenAI Ads Conversions API nesta sessão).

- [ ] **Step 3: Deploy do código**

Via SSH:
```bash
cd /root/dexidigital.com.br && git pull origin master && npm run build && pm2 restart dexidigital
```

- [ ] **Step 4: Rodar a ingestão manualmente pela primeira vez na VPS**

Via SSH:
```bash
cd /root/dexidigital.com.br && node --env-file=.env.local scripts/ingest-news.mjs
```
Expected: mesmo tipo de log da Task 3 Step 4, confirmando que funciona no ambiente de produção (não só localmente).

- [ ] **Step 5: Configurar o cron diário na VPS**

Via SSH, adicionar ao crontab do root (`crontab -e`, ou `(crontab -l; echo "...") | crontab -`):
```
0 6 * * * cd /root/dexidigital.com.br && node --env-file=.env.local scripts/ingest-news.mjs >> logs/news-ingest.log 2>&1
```
(criar o diretório `logs/` na VPS se não existir: `mkdir -p /root/dexidigital.com.br/logs`)

- [ ] **Step 6: Verificar em produção**

Run: `curl -s https://dexidigital.com.br/noticias -o /dev/null -w "HTTP %{http_code}\n"`
Expected: `HTTP 200`.

Conferir visualmente (navegador) que `https://dexidigital.com.br/noticias` mostra o grid de notícias e que "Notícias" aparece no rodapé do site em produção.

- [ ] **Step 7: Conferir o crontab foi salvo corretamente**

Run (via SSH): `crontab -l`
Expected: a linha do Step 5 presente na listagem.
